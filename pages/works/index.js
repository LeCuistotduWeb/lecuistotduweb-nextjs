import Head from 'next/head'
import DefaultLayout from "../../layouts/DefaultLayout";
import WorkCard from "../../components/cards/WorkCard";
import useSWR from "swr";
import siteConfig from "../../siteConfig";
import Link from "next/link";

const fetcher = (...args) => fetch(...args).then((res) => res.json())

const WorksPage = ({works}) => {

  const { data, error } = useSWR(`/api/works`, fetcher)

  return (
      <DefaultLayout>
          <Head>
              <title>{siteConfig.title} - Réalisations</title>
              <meta name="description" content="Generated by create next app" />
              <link rel="icon" href="/favicon.ico" />
          </Head>

          <main className="container works">
              <h1 className="section-title">Réalisations</h1>
              <ul className="works-container">
                  {data && data.map((work, index) => (
                      <li key={index}>
                          <WorkCard work={work}/>
                      </li>
                  ))}
              </ul>

              <div>
                  <p>Retrouvez mon parcours et mes diverses experiences <Link href="/experiences">ici</Link></p>
                  <p>Suivez-moi aussi sur mon <a rel="noopener" href="https://codepen.io/lecuistotduweb">Codepen</a>. Ou sur mon github <a rel="noopener" href="https://github.com/lecuistotduweb">LeCuistotduWeb</a>.</p>
              </div>
          </main>

      </DefaultLayout>
  )
}

export default WorksPage
